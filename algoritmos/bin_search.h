#ifndef BIN_SEARCH_H
#define BIN_SEARCH_H

int bin_search_int(int flag, int *vector, int size);
int bin_search_float(float flag, float *vector, int size);
int bin_search_char(char flag, char *vector, int size);

#endif